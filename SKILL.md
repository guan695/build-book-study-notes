---
name: build-book-study-notes
description: 先询问学习基础、可检验的掌握目标、时间安排和讲解偏好，再从开放互联网核验并下载合法书籍，按模块裁剪相关 PDF 页段，只依据实际阅读的本地页段生成可深入学习的中文笔记。适用于教材导读、技术学习和系统复习；不适用于不要求书籍依据的普通网页总结。
---

# 可靠书籍学习笔记

目标不是快速交付一份提纲，而是让用户达到约定的、可以检验的掌握程度。整次任务逐模块自动执行；每次只把当前模块需要的小 PDF 放入上下文。

## 输出位置

在当前可写工作目录创建一次且仅一次：

```text
notes/<YYYYMMDD>-<总主题>/
├── sources.json
├── materials/
├── extracts/
└── notes/
    ├── 01-<知识点>.md
    ├── 02-<知识点>.md
    └── ...
```

所有模块共享 `sources.json`、`materials/` 和 `extracts/`。不创建模块子目录，不生成根目录 `notes.md`。除非用户要求，不生成书单报告、证据卡或其他永久过程文件。

## 一、先达成学习共识

1. 在联网和创建文件前，一次性询问缺少的信息：
   - 学习主题和边界；
   - 已掌握的先备知识；
   - 学完后要能完成什么，例如“能解释”“能推导”“能独立解题”“能用于项目”或“能阅读论文”；
   - 可用时间或期望节奏；
2. 不得静默代入默认值。用户没有回答时停止等待。
3. 按先备依赖和可检验学习目标拆分模块。每个模块只承担一个连贯目标，模块数量不设上下限；范围太大就继续拆分，不得把多个庞大主题塞进一篇短笔记。
4. 简短说明完整路线和每个模块学完能做什么，只请求一次整体确认。确认后自动逐个完成，不再让用户逐模块选择。
5. 时间只决定节奏和路线优先级，不降低达到目标所需的知识完整性。用户说“不限时间”时，必须保留必要先备知识、解释、推理、例子、边界情况和练习，不得为了尽快结束而压缩成概览。

## 二、寻找并下载资料

1. 完整阅读 [来源规则](references/source-policy.md)。实时联网寻找真实书籍，不依赖模型记忆。
2. 初次可比较 3–5 本候选书。为每个模块选最合适的主来源，并只在确有缺口时加入补充来源；不同模块可以使用不同主书，整条路线不设武断的书籍数量上限。
3. 每本实际使用的书至少由两个独立网站核验，其中一个必须是出版社、作者、大学、专业机构或图书馆等权威来源。
4. 只从合法公开地址下载完整正文到全局 `materials/`。已有相同文件时核对哈希后复用。
5. 在全局 `sources.json` 中记录路线、用户目标、书目、核验链接、下载地址、许可、SHA-256、用途和页段。没有合法全文时停止，不得用摘要、零售页或模型记忆补写正文。

## 三、逐模块深读并写作

对 `M01`、`M02`……依次执行以下循环，不得为了在一次回复内完成全部路线而降低当前模块深度。

1. 在 `tmp/Mxx-coverage.md` 建立临时覆盖表，至少列出：学习结果、先备知识、待讲内容、对应书籍与页段、需要的解释/推导/例子/应用，以及“已覆盖/缺口”状态。
2. 用目录、索引和关键词定位当前模块需要的完整连续页段，不设总页数上限。把页段写入 `sources.json`，ID 使用 `Mxx-Bxx-Sxx`，然后运行：

   ```powershell
   python -X utf8 scripts/book_manifest.py <sources.json> --check
   ```

3. 根据本机 PDF 工具，在本次学习目录的 `tmp/` 临时编写最小裁剪程序。程序从 `sources.json` 读取原书路径、页段 ID 和页码，批量生成 `extracts/<页段ID>.pdf`；不得写死书名、主题或页码。必须复制原 PDF 页面，保留公式、图片和版式，不能替换成 TXT、OCR 文本或截图。
4. 分批阅读所有当前页段，避免一次载入整本书。第一遍提取定义、论证、步骤、例子、图表和限制；第二遍按覆盖表综合内容并查缺。公式、符号、图片或版式有意义时必须查看原 PDF 页面。
5. 覆盖表仍有关键缺口时，继续提取页段、增加合法来源或拆分模块，不能凭记忆补齐。只有覆盖完成后，才按照 [笔记规则](references/note-template.md) 写 `notes/<序号>-<知识点>.md`。
6. 写完执行质量门槛：
   - 概念和符号均在使用前解释，先备关系清楚；
   - 约定的学习结果全部有正文支撑；
   - 关键推导不跳过影响理解的步骤，代表性任务可由目标学习者独立复现；
   - 每个关键结论、公式和方法都来自实际阅读的本地页段；
   - 内容不是“一段话一个概念”的压缩提纲；
   - 所有数学公式和符号都使用适合 Markdown 的 LaTeX 写法。
7. 未通过就扩写、补资料或拆分后重写；通过后自动继续下一模块。全部完成后把 `phase` 设为 `complete`，删除 `tmp/`，统一交付 `notes/` 中的文件，不另行合并。

## 不可放宽

- 不使用盗版、影子图书馆或绕过付费墙的内容。
- 不虚构书名、版本、ISBN、页码、引文或未读内容。
- 搜索摘要、零售页和用户评论不能支撑笔记正文。
- 保留原书不变；优先转述，必要引文保持简短并注明位置。
- 来源冲突时说明差异，不擅自拼成一个结论。
